import classes from "./style.module.css";

const Footer = () => {
    return (
        <footer className={classes.footer}>
            <h2>@Условно powered by NEVROKS</h2>
        </footer>
    );
};

export default Footer;